# Load driver
sudo modprobe uvcvideo
# Check if visible
lsusb
v4l2-ctl --list-devices
# Set format
v4l2-ctl -d /dev/video0 --set-fmt-video=width=320,height=320,pixelformat=MJPG
# Check avaliable controls, look for white_balance_automatic
v4l2-ctl -d /dev/video0 --list-ctrls
